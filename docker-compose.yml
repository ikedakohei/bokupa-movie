version: "3.8"

services:
  web:
    build:
      context: .
      dockerfile: ./Dockerfile
    tty: true
    volumes:
      - type: bind
        source: .
        target: /bokupa-movie
    command: sh -c "sh entrypoint.dev.sh"
    ports:
      - "5173:5173"
    environment:
      TZ: Asia/Tokyo
